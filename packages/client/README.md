# @devshell/client

Stub package. Later: types + thin `fetch` helpers for the Devshell loopback API (`getGitStatus`, `openPreview`). Degrades to no-ops when Devshell is absent.

Not implemented in R0.
